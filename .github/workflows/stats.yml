name: GitHub Stats

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
  push:
    branches:
      - master

permissions:
  contents: write

# Run the JS wrapper actions (checkout/setup-node) on Node 24 now; their
# current releases still bundle Node 20, which GitHub deprecates after
# 2026-06-02. This also future-proofs past that date.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

# Never let two stats runs race on the same branch
concurrency:
  group: github-stats
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Generate stats
        run: npm run generate
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          EXCLUDED: ${{ secrets.EXCLUDED }}
          EXCLUDED_LANGS: ${{ secrets.EXCLUDED_LANGS }}
          EXCLUDE_FORKED_REPOS: "true"

      - name: Commit generated SVGs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add generated
          git commit -m "Update generated stats" || echo "No changes"
          git push
