{
  "name": "github-stats",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-owned GitHub profile statistics generator (clean-room TypeScript, GraphQL-only).",
  "engines": { "node": ">=20" },
  "scripts": {
    "generate": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "octokit": "^4.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
